import { useDiary } from "@/lib/imanote/diary-context";
import { isValidLockValue } from "@/lib/imanote/validation";
import type { DiaryEntry, LockKind } from "@/lib/imanote/types";
import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { useAudioPlayer, useAudioPlayerStatus } from "expo-audio";
import { useEffect, useRef, useState } from "react";
import { Animated, Easing, PanResponder, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import Svg, { Circle, Path } from "react-native-svg";

function Flower({ size = 100, color = "#E989A7", locked = false }: { size?: number; color?: string; locked?: boolean }) { return <Svg width={size} height={size} viewBox="0 0 100 100">{[0, 72, 144, 216, 288].map((rotation) => <Path key={rotation} d="M50 50 C15 34 20 4 50 17 C80 4 85 34 50 50" fill={color} transform={`rotate(${rotation} 50 50)`} />)}<Circle cx="50" cy="50" r="15" fill="#F5D481" />{locked ? <><Path d="M40 53V47a10 10 0 0 1 20 0v6" stroke="#785E44" strokeWidth="5" strokeLinecap="round" fill="none" /><Path d="M36 51h28v22H36z" fill="#785E44" /><Circle cx="50" cy="62" r="3" fill="#F5D481" /></> : <Circle cx="50" cy="50" r="5" fill="#C65B7C" />}</Svg>; }
type Petal = { id: number; x: number; y: number; size: number; color: string; rotation: number };
export function FloralSplash({ onFinish }: { onFinish: () => void }) { const [petals, setPetals] = useState<Petal[]>([]); const opacity = useRef(new Animated.Value(1)).current; const count = useRef(0); useEffect(() => { const timeout = setTimeout(() => Animated.timing(opacity, { toValue: 0, duration: 380, easing: Easing.out(Easing.quad), useNativeDriver: true }).start(onFinish), 1500); return () => clearTimeout(timeout); }, [onFinish, opacity]); const bloom = (x: number, y: number) => setPetals((items) => [...items.slice(-35), ...[0, 1, 2].map((i) => ({ id: count.current++, x: x + (i - 1) * 15, y: y + (i % 2 ? -7 : 7), size: 23 + i * 7, color: ["#F7C7D5", "#E989A7", "#F0A8BB"][i], rotation: i * 25 }))]); const responder = useRef(PanResponder.create({ onStartShouldSetPanResponder: () => true, onMoveShouldSetPanResponder: () => true, onPanResponderGrant: (e) => bloom(e.nativeEvent.locationX, e.nativeEvent.locationY), onPanResponderMove: (e) => bloom(e.nativeEvent.locationX, e.nativeEvent.locationY) })).current; return <Animated.View style={[s.splash, { opacity }]} {...responder.panHandlers}><View style={s.haloA} /><View style={s.haloB} />{petals.map((petal) => <View key={petal.id} style={{ position: "absolute", left: petal.x - petal.size / 2, top: petal.y - petal.size / 2, transform: [{ rotate: `${petal.rotation}deg` }] }}><Flower size={petal.size} color={petal.color} /></View>)}<View style={s.splashFlower}><Flower size={116} /></View></Animated.View>; }
export function PrivacyGate() { const { accessState, configureLock, unlock, copy, palette, isRTL } = useDiary(); const [kind, setKind] = useState<LockKind>("pin"); const [first, setFirst] = useState(""); const [confirm, setConfirm] = useState(""); const [error, setError] = useState(""); const setup = accessState === "setup"; const pin = kind === "pin"; const active = setup && first.length === 4 ? confirm : first; const submit = async () => { setError(""); if (setup) { if (!isValidLockValue(kind, first)) return setError(copy.invalidPin); if (first !== confirm) return setError(copy.mismatch); await configureLock(kind, first); } else if (!(await unlock(first))) setError(copy.wrongLock); }; const digit = (value: string) => { if (value === "⌫") return setup && first.length === 4 ? setConfirm((v) => v.slice(0, -1)) : setFirst((v) => v.slice(0, -1)); if (first.length < 4) setFirst((v) => `${v}${value}`); else if (setup && confirm.length < 4) setConfirm((v) => `${v}${value}`); }; return <View style={[s.gate, { backgroundColor: palette.background }]}><View style={[s.gateBubble, { backgroundColor: palette.primarySoft }]} /><View style={s.gateContent}><Flower size={92} color={palette.flower} locked /><Text style={[s.gateTitle, { color: palette.text, textAlign: isRTL ? "right" : "left" }]}>{setup ? copy.setupTitle : copy.locked}</Text><Text style={[s.gateBody, { color: palette.muted, textAlign: isRTL ? "right" : "left" }]}>{setup ? copy.setupBody : copy.localOnly}</Text>{setup && <View style={s.segments}>{(["pin", "password"] as LockKind[]).map((option) => <Pressable key={option} onPress={() => { setKind(option); setFirst(""); setConfirm(""); setError(""); }} style={[s.segment, { borderColor: palette.border, backgroundColor: kind === option ? palette.primary : palette.surface }]}><Text style={{ color: kind === option ? "#fff" : palette.text, fontWeight: "800" }}>{option === "pin" ? copy.choosePin : copy.choosePassword}</Text></Pressable>)}</View>}{pin ? <View style={s.pinBlock}><Text style={[s.hint, { color: palette.muted }]}>{setup ? first.length < 4 ? copy.choosePin : copy.confirmPin : copy.enterPin}</Text><View style={s.dots}>{[0, 1, 2, 3].map((item) => <View key={item} style={[s.dot, { backgroundColor: item < active.length ? palette.primary : palette.border }]} />)}</View><View style={s.keypad}>{["1", "2", "3", "4", "5", "6", "7", "8", "9", "", "0", "⌫"].map((key) => <Pressable key={key || "blank"} disabled={!key} onPress={() => digit(key)} style={({ pressed }) => [s.key, { backgroundColor: key ? palette.surface : "transparent", opacity: pressed ? 0.7 : 1 }]}><Text style={[s.keyText, { color: palette.text }]}>{key}</Text></Pressable>)}</View></View> : <View style={s.passwordBlock}><TextInput value={first} onChangeText={setFirst} placeholder={setup ? copy.choosePassword : copy.enterPassword} placeholderTextColor={palette.muted} secureTextEntry autoCapitalize="none" style={[s.input, { backgroundColor: palette.surface, color: palette.text, borderColor: palette.border, textAlign: isRTL ? "right" : "left" }]} />{setup && <TextInput value={confirm} onChangeText={setConfirm} placeholder={copy.confirmPassword} placeholderTextColor={palette.muted} secureTextEntry autoCapitalize="none" style={[s.input, { backgroundColor: palette.surface, color: palette.text, borderColor: palette.border, textAlign: isRTL ? "right" : "left" }]} />}</View>}{!!error && <Text style={[s.error, { color: palette.danger }]}>{error}</Text>}<Pressable onPress={() => void submit()} style={({ pressed }) => [s.primary, { backgroundColor: palette.primary, opacity: pressed ? 0.85 : 1 }]}><Text style={s.primaryText}>{setup ? copy.createLock : copy.unlock}</Text></Pressable></View></View>; }
export function formatDiaryDate(value: string, language: "ar" | "fr" | "en") { return new Intl.DateTimeFormat(language === "ar" ? "ar-DZ" : language === "fr" ? "fr-FR" : "en-US", { day: "numeric", month: "short", year: "numeric" }).format(new Date(value)); }
export function EntryCard({ entry, onPress }: { entry: DiaryEntry; onPress: () => void }) { const { palette, settings, isRTL, fontFamily } = useDiary(); return <Pressable onPress={onPress} style={({ pressed }) => [s.card, { backgroundColor: palette.surface, borderColor: palette.border, opacity: pressed ? 0.72 : 1 }]}><View style={[s.cardRow, { flexDirection: isRTL ? "row-reverse" : "row" }]}><View style={[s.miniFlower, { backgroundColor: palette.primarySoft }]}><MaterialIcons name="auto-awesome" size={16} color={palette.primary} /></View><View style={s.cardCopy}><Text numberOfLines={1} style={[s.cardTitle, { color: palette.text, textAlign: isRTL ? "right" : "left", fontFamily: fontFamily(entry.font) }]}>{entry.title || "—"}</Text><Text numberOfLines={2} style={[s.cardBody, { color: palette.muted, textAlign: isRTL ? "right" : "left", fontFamily: fontFamily(entry.font) }]}>{entry.body.replace(/\s+/g, " ").trim() || "…"}</Text><View style={[s.cardMeta, { flexDirection: isRTL ? "row-reverse" : "row" }]}><Text style={[s.cardDate, { color: palette.muted }]}>{formatDiaryDate(entry.updatedAt, settings.language)}</Text>{entry.audioUri && <MaterialIcons name="mic-none" color={palette.primary} size={15} />}{entry.attachments?.length ? <MaterialIcons name="photo" color={palette.primary} size={15} /> : null}</View></View></View></Pressable>; }
export function AudioPlayback({ uri, durationMs }: { uri: string; durationMs?: number }) { const { copy, palette, isRTL } = useDiary(); const player = useAudioPlayer({ uri }); const status = useAudioPlayerStatus(player); const seconds = Math.max(0, Math.floor(status.duration || (durationMs ?? 0) / 1000)); const toggle = () => { if (status.playing) player.pause(); else { if (status.currentTime >= status.duration && status.duration > 0) player.seekTo(0); player.play(); } }; return <View style={[s.playback, { backgroundColor: palette.softSurface, borderColor: palette.border, flexDirection: isRTL ? "row-reverse" : "row" }]}><Pressable onPress={toggle} style={[s.playButton, { backgroundColor: palette.primary }]}><MaterialIcons name={status.playing ? "pause" : "play-arrow"} color="#fff" size={24} /></Pressable><View style={{ flex: 1 }}><Text style={[s.playTitle, { color: palette.text, textAlign: isRTL ? "right" : "left" }]}>{copy.voiceNote}</Text><Text style={[s.playInfo, { color: palette.muted, textAlign: isRTL ? "right" : "left" }]}>{status.playing ? copy.pause : copy.play} · {Math.floor(seconds / 60)}:{String(seconds % 60).padStart(2, "0")}</Text></View></View>; }
const s = StyleSheet.create({ splash: { flex: 1, backgroundColor: "#C7D1F0", overflow: "hidden" }, haloA: { position: "absolute", width: 330, height: 330, borderRadius: 165, backgroundColor: "#DEE5FA", top: 94, left: -74 }, haloB: { position: "absolute", width: 300, height: 300, borderRadius: 150, backgroundColor: "#F8DCE6", bottom: 40, right: -100, opacity: 0.52 }, splashFlower: { position: "absolute", top: "43%", left: "50%", transform: [{ translateX: -58 }, { translateY: -58 }] }, gate: { flex: 1, justifyContent: "center", padding: 24, overflow: "hidden" }, gateBubble: { position: "absolute", width: 350, height: 350, borderRadius: 175, right: -125, top: -135 }, gateContent: { width: "100%", maxWidth: 430, alignSelf: "center", gap: 17 }, gateTitle: { fontSize: 29, lineHeight: 37, fontWeight: "800" }, gateBody: { fontSize: 15, lineHeight: 23 }, segments: { flexDirection: "row", gap: 9 }, segment: { flex: 1, minHeight: 48, borderWidth: 1, borderRadius: 15, alignItems: "center", justifyContent: "center", paddingHorizontal: 8 }, pinBlock: { gap: 15 }, hint: { textAlign: "center", fontSize: 14, fontWeight: "700" }, dots: { flexDirection: "row", justifyContent: "center", gap: 14 }, dot: { width: 13, height: 13, borderRadius: 7 }, keypad: { flexDirection: "row", flexWrap: "wrap", justifyContent: "center", gap: 10 }, key: { width: "29%", aspectRatio: 1.28, borderRadius: 17, alignItems: "center", justifyContent: "center" }, keyText: { fontSize: 23, fontWeight: "700" }, passwordBlock: { gap: 12 }, input: { minHeight: 54, paddingHorizontal: 16, fontSize: 16, borderRadius: 16, borderWidth: 1 }, error: { textAlign: "center", fontSize: 14, fontWeight: "700" }, primary: { minHeight: 54, borderRadius: 17, alignItems: "center", justifyContent: "center" }, primaryText: { color: "#fff", fontSize: 16, fontWeight: "800" }, card: { borderWidth: 1, borderRadius: 20, padding: 15, marginBottom: 11 }, cardRow: { gap: 12, alignItems: "flex-start" }, miniFlower: { width: 35, height: 35, borderRadius: 18, alignItems: "center", justifyContent: "center" }, cardCopy: { flex: 1, gap: 5 }, cardTitle: { fontSize: 17, lineHeight: 23, fontWeight: "800" }, cardBody: { fontSize: 14, lineHeight: 20 }, cardMeta: { alignItems: "center", gap: 8, marginTop: 2 }, cardDate: { fontSize: 12, fontWeight: "600" }, playback: { borderWidth: 1, borderRadius: 18, padding: 12, alignItems: "center", gap: 12 }, playButton: { width: 43, height: 43, borderRadius: 22, alignItems: "center", justifyContent: "center" }, playTitle: { fontSize: 14, fontWeight: "800" }, playInfo: { fontSize: 13, marginTop: 3 } });
